import { css } from '@emotion/react';

export const imageContainer = css`
  margin: 0 0.4rem;
`;

export const mainContent = css`
  display: flex;
  padding: 0.4rem;

  .main-content {
    flex-grow: 1;
    padding: 0 50px;
    margin-top: 3rem;
  }

  aside {
    margin-top: 2rem;
  }
`;
