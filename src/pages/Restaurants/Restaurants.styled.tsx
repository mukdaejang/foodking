import { css } from '@emotion/react';

export const imageContainer = css`
  margin: 0 0.4rem;
`;

export const mainContent = css`
  display: flex;

  .main-content {
    flex-grow: 1;
    padding: 0 22rem;
    margin-top: 4rem;
  }

  aside {
    width: 25rem;
  }
`;
